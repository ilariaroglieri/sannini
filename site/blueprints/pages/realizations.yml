title: Realizations

sections:
  realizations_list:
    type: pages
    label:
      it: Realizations
      en: Realizations
    parent: site.find("realizzazioni")
    templates:
      - realization
    image:
      query: page.cover_img.toFile
    empty: No realizations. Click "+" to add one.
