title: Prodotti

columns:
  - width: 2/3
    sections:
      contents:
        headline: Page contents
        type: fields
        fields:
          page_title:
            type: text
            
          intro_text:
            extends: fields/writer-basic

  - width: 1/3
    sections:
      products_list:
        type: pages
        label: Products
        parent: site.find("prodotti")
        templates:
          - product
        layout: list
        image:
          query: page.cover_img.toFile
        empty: No products. Click "+" to add one.

